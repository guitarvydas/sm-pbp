mr2json {
  Top = name "{" Array+ "}"
  Array = name "{" attr* Object+ "}"
  attr = name "=" str
  Object = name "{" attr+ "}"
  name = uletter ualnum*
  uletter = letter | "_"
  ualnum = alnum | "_"
  str = "\"" (~"\"" any)* "\""
}
